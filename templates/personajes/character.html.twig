{% extends "base.html.twig" %}

{% block body %}
<h2>El personaje con id {{id}} </h2>
<section>
    <h3>{{character.name}}</h3>
    <p>Planeta: {{character.planet}}</p>
    <p>Superpoder: {{character.superpower}}</p>
    <p>Descripción: {{character.description}}</p>
    <p>Fecha de nacimiento: {{character.BirthDate|date("Y-m-d")}}</p>
    <ul>Debilidades:
        {% for weakness in character.weaknesses %}
        <li>{{weakness.name}}</li>
            
        {% endfor %}
    </ul>
</section>
{% endblock %}
{% block footer %}
<h2>Footer</h2>
{% endblock %}